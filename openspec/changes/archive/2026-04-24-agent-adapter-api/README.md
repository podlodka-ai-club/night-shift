# agent-adapter-api

Provider-agnostic agent adapter (session-based) with a Codex adapter implementation, role/model config loader, and wiring of cost/token metrics into AgentInvoked observability events.
